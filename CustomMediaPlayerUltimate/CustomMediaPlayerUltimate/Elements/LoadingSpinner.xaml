<UserControl x:Class="CustomMediaPlayerUltimate.Elements.LoadingSpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomMediaPlayerUltimate.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="40">
    <ContentControl Content="{StaticResource LoadingIcon}" RenderTransformOrigin="0.5,0.5">
        <ContentControl.Resources>
            <Storyboard x:Key="LoadingAnimation" x:Shared="False">
                <DoubleAnimation Storyboard.TargetProperty="(ContentControl.RenderTransform).(RotateTransform.Angle)"
                     From="0.0"
                     To="360.0"
                     Duration="0:0:1"
                     RepeatBehavior="Forever">
                    <DoubleAnimation.EasingFunction>
                        <QuadraticEase EasingMode="EaseInOut"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
        </ContentControl.Resources>
        <ContentControl.RenderTransform>
            <RotateTransform Angle="0"/>
        </ContentControl.RenderTransform>
        <ContentControl.Triggers>
            <EventTrigger RoutedEvent="ContentControl.Loaded">
                <BeginStoryboard Storyboard="{StaticResource LoadingAnimation}"/>
            </EventTrigger>
        </ContentControl.Triggers>
    </ContentControl>
</UserControl>
