<UserControl x:Class="CustomMediaPlayerUltimate.Elements.CustomSongElement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomMediaPlayerUltimate.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200">
    <Grid x:Name="GridContainer" Style="{StaticResource CustomSongElement}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>
        <Label x:Name="ItemTextLabel" Grid.Column="0" VerticalAlignment="Center" Foreground="WhiteSmoke"/>
        <Button x:Name="PlayButton" Grid.Column="1" Style="{StaticResource CustomButton}" Width="28" Height="28" Margin="2,2,-6,2" Click="PlayButtonClick">
            <Image Source="/Images/play.png"/>
        </Button>

        <Grid.ContextMenu>
            <ContextMenu>
                <ContextMenu.Items>
                    <MenuItem Header="Add lyrics..." Click="AddLyricsMenuItemClick"/>
                    <MenuItem Header="Play as next song" Click="PlayAsNextSongMenuItemClick"/>
                </ContextMenu.Items>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</UserControl>
